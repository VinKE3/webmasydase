/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "aceternity.com"], // Agrega aquí los dominios permitidos
  },
};

export default nextConfig;
